---
title: Joe Masilotti, the Turbo Native guy
description: |
  I help businesses build and launch hybrid iOS apps powered by Turbo Native and
  Ruby on Rails.

---

<%= render UI::Container.new(class_name: "mt-16 sm:mt-32") do %>
  <div class="grid grid-cols-1 gap-y-16 lg:grid-cols-2 lg:grid-rows-[auto_1fr] lg:gap-y-12">
    <div class="lg:pl-20">
      <div class="max-w-[250px] px-2.5 lg:max-w-none">
        <figure>
          <%= image_tag "joe.jpg", alt: "Joe Masilotti", class: "aspect-square sm:rotate-3 rounded-2xl bg-zinc-100 object-cover" %>
          <figcaption class="sr-only">Joe Masilotti</figcaption>
        </figure>
      </div>
    </div>

    <div class="lg:order-first lg:row-span-2">
      <h1 class="text-4xl font-bold tracking-tight text-zinc-800 sm:text-5xl">I'm <%= current_page.data["title"] %>.</h1>

      <div class="prose">
        <%= render "partials/about" %>
      </div>
    </div>

    <div class="lg:pl-20">
      <ul role="list">
        <%= render WideSocialLink.new(href: data.socials.email.href, label: data.socials.email.handle, icon: data.socials.email.icon) %>

        <%= render WideSocialLink.new(href: data.socials.twitter.href, label: data.socials.twitter.title, icon: data.socials.twitter.icon, class_name: "mt-8 border-t border-zinc-100 pt-8") %>
        <%= render WideSocialLink.new(href: data.socials.github.href, label: data.socials.github.title, icon: data.socials.github.icon, class_name: "mt-4") %>
        <%= render WideSocialLink.new(href: data.socials.mastodon.href, label: data.socials.mastodon.title, icon: data.socials.mastodon.icon, class_name: "mt-4") %>
        <%= render WideSocialLink.new(href: data.socials.linkedin.href, label: data.socials.linkedin.title, icon: data.socials.linkedin.icon, class_name: "mt-4") %>
      </ul>
    </div>
  </div>
<% end %>
