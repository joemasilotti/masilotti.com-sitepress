---
title: Archives
description: |
  Everything I've ever published on my site, in reverse chronological order.
image: masilotti.com.png

---

<%= render UI::SimpleLayout.new(
  title: current_page.data.title,
  intro: current_page.data.description
) do %>
  <div class="md:border-l md:border-zinc-100 md:pl-6">
    <div class="flex max-w-3xl flex-col space-y-16">
      <% ArticlePage.all.each do |article| %>
        <%= render Article.new(article, wide: true) %>
      <% end %>
    </div>
  </div>
<% end %>
