<nav class="pointer-events-auto hidden md:block">
  <ul class="flex rounded-full bg-white/90 px-3 text-sm font-medium text-zinc-800 shadow-lg shadow-zinc-800/5 ring-1 ring-zinc-900/5 backdrop-blur">
    <%= render Navigation::Item.new("About", page_path("about"), request_path:) %>
    <%= render Navigation::Item.new("Articles", page_path("articles"), request_path:) %>
    <%= render Navigation::Item.new("Projects", "/projects", request_path:) %>
    <%= render Navigation::Item.new("Speaking", "/speaking", request_path:) %>
    <%= render Navigation::Item.new("Uses", "/uses", request_path:) %>
  </ul>
</nav>
